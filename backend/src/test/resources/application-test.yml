spring:
  datasource:
    url: jdbc:h2:mem:aes;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration,filesystem:../database/migration

aes:
  auth:
    initial-admin:
      username: admin
      password: Admin@123456
      real-name: 测试管理员
